spring:
  # 스프링부트는 기본적으로 별도의 설정이 없으면 메모리모드로 돌림
#  datasource:
#    url: jdbc:h2:mem:test #메모리DB
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop #이미 있으면 drop하고 create함 그리고 종료 전에 다 drop함, 스프링부트는 기본적으로 create-drop임
#    properties:
#      hibernate:
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug #logger로 찍어줌
#    org.hibernate.type: trace